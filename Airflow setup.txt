sudo apt-get install python3.8 python3.8-distutils python3-pip

sudo apt-get update
sudo apt-get install software-properties-common
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt-get updatey

sudo apt-get install python3.8 python3.8-distutils python3-pip


make python 3.8 default:
sudo update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.12 1
sudo update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.8 2
sudo update-alternatives --config python3


pip install airflow==2.8.4

cd ~/airflow_project
source airflow-venv/bin/activate  #Activate virtual env

airflow db init
airflow users create \
    --username admin \
    --firstname David \
    --lastname Ibanga \
    --role Admin \
    --email dibanga2800@gmail.com \
    --password D030avbang@@
	
	
airflow webserver

run webserver and scheduler:
airflow webserver -p 8080 & airflow scheduler



Note in venv
install pandas, dotnet env, azure storage blob

